#include <stdio.h>

int main(void) {

	int T;
	scanf("%d", &T);
	int A, B;
	int sum[100] = {0};
	for (int i = 0; i < T; i++) {
		scanf("%d %d", &A, &B);
		sum[i] = A + B;
	}
	for (int i = 0; i < T; i++) {
		printf("%d\n", sum[i]);
	}

	return 0;
}
