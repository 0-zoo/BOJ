#include <stdio.h>

int main(void) {

	int year;
	int flag = 0;
	scanf("%d", &year);

	if (year % 4 == 0) {
		if (year % 100 != 0) {
			flag = 1;
		}
		else if (year % 400 == 0) {
			flag = 1;
		}
	}
	printf("%d", flag);

	return 0;
}
