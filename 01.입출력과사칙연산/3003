#include <stdio.h>

int main(void) {
	int a, b, c, d, e, f;
	int a1, b1, c1, d1, e1, f1;

	scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &e, &f);
	a1 = 1 - a;
	b1 = 1 - b;
	c1 = 2 - c;
	d1 = 2 - d;
	e1 = 2 - e;
	f1 = 8 - f;
	printf("%d %d %d %d %d %d", a1, b1, c1, d1, e1, f1);

	return 0;
}
