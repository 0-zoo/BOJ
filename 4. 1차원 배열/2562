#include <stdio.h>

int main(void) {
	int arr[9] = { 0, };
	int max = 0;
	int k = 0;
	for (int i = 0; i < 9; i++) {
		scanf("%d", &arr[i]);
	}
	for (int i = 0; i < 9; i++) {
		if (max < arr[i]) {
			max = arr[i];
			k = i;
		}
	}
	printf("%d\n", max);
	printf("%d\n", k + 1);

	return 0;
}
